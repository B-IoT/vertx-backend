orbs:
  gradle: circleci/gradle@2.2.0
version: 2.1
machine: true      
workflows:
  checkout-build-test:
    jobs:
      - run:
          name: Start container and verify it's working
          command: |
            set -x
            docker-compose up -d
      - gradle/test